# 运行产物（绝不入库）
# dsh-test-home/ 是插件启动时利用部署环境构建的测试实例目录（profiles/sessions/storages/skills 等），
# 来源见 components/git-rescue/lib/test-env-entry.js 与 skills/dsh-test-env.md，只存在于本机，不上传 GitHub。
dsh-test-home/

# 依赖与临时产物
node_modules/
*.log
dsh-test-rc7/
# npm 下载产物（dsh-git-push 自动追加，2026-08-20）
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lock
npm-debug.log*
.npm/
.pnpm-store/
